# Alt-Tab Hide - GNOME Shell Extension

A GNOME Shell extension that allows you to hide specific applications from the Alt-Tab window switcher.

### Development Shell

To test without affecting your current session:

```bash
dbus-run-session gnome-shell --devkit --wayland
```
